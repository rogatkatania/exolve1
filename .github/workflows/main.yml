ssh user@familiar-reading 'mkdir -p ~/exolve1/.github/workflows && echo -e "name: Familiar Reading CI\n\non: [push, pull_request]\n\njobs:\n  build:\n    runs-on: familiar-reading\n\n    steps:\n    - name: Checkout the repository\n      uses: actions/checkout@v3\n\n- name: Navigate to exolve1 directory and install dependencies\n      run: |\n        cd && cd root && cd exolve1\n        npm install\n        \n    - name: Build the project\n      run: |\n        npm run build\n        \n    - name: Run development server\n      run: |\n        cd exolve1\n        npm run dev -- --host\n\n    - name: Setup loclx tunnel\n      run: loclx tunnel http --reserved-domain sippan3.loclx.io --to 217.144.187.254:443"
